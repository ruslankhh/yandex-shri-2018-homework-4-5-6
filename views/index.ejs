<% include ./partials/header %>
  <ul>
    <% tree.children.forEach(file => { %>
      <li>
        <a href="/<%= `${file.type}/${branch}/${file.filepath}` %>">
          <%= file.base %>
        </a>
      </li>
    <% }); %>
  </ul>
<% include ./partials/footer %>
